---
layout: default
---

<div class="home">

  <ul class="post-list">
    {% for post in site.posts%}
      <li>
				{% if post.category == "tech" %}
        <h2>
          <a class="post-link" href="{{ post.url | prepend: site.baseurl }}">{{ post.title }}</a>
					<p class="post-meta">{{ post.date | date: "%b %-d, %Y" }} {% if post.summary %}• {{ post.summary}}{% endif %}</p>
        </h2>
				{% endif %}

      </li>
    {% endfor %}
  </ul>

</div>
